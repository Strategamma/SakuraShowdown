Drop custom GLB models here to replace the procedural pieces.

Expected filenames:
- master.glb
- student.glb

Each model should be centered at the origin and roughly fit within a 1x1x1 box.
The renderer will scale them to match the board cell size.
